piano/node_modules
node_modules
build/
public/gbAnimation/
.idea/workspace.xml
yarn.lock
.idea/
build_log.txt
install_log.txt
start_log.txt
# Env files — никогда не коммитим (содержат пароли и токены)
.env
.env.local
.env.*.local

# Тяжёлые файлы — >100 МБ не влезают в GitHub. Хранить на внешнем CDN.
public/albums/*.zip
public/albums/*.rar
public/albums/*.7z

