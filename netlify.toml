[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  CI = "false"

[functions]
  directory = "netlify/functions"

# SPA fallback — все пути отдают React index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
